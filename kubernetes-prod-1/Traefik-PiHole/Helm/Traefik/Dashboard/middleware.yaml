apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: traefik-dashboard-basicauth
  namespace: traefik
spec:
  basicAuth:
    secret: traefik-dashboard-auth
# ---
# apiVersion: traefik.io/v1alpha1
# kind: Middleware
# metadata:
#   name: redirect-to-https
#   namespace: traefik  # ✅ Add the namespace (important if not applying to default)
# spec:
#   redirectScheme:
#     scheme: https        # ✅ Required
#     permanent: true
